# Closeout Receipt

- Result: COMPLETE / PARTIAL / BLOCKED
- Changed:
- Explicitly unchanged / non-goals:
- Base SHA:
- Verified HEAD or workspace fingerprint:
- Verification commands and results:
- Databases / providers / platforms actually exercised:
- PR and CI readback:
- Migration / deployment / runtime readback:
- Public smoke and observed result:
- Rollback evidence:
- Blocked or unverified surfaces:
- Remaining risk:
- Worktree disposition: keep / deletion candidate with evidence

Use `NOT APPLICABLE` rather than deleting a field. Never write `VERIFIED` for a
surface inferred from a different gate.
